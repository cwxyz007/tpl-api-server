export enum ErrorCode {
  unknown,
  success,
  invalidPassword,
  invalidUsername,
  loginFailed,
  duplicateUsername,
  invalidRegisterCode,
  invalidToken,
  exist,
  notExist,
  permissionDenied
}
